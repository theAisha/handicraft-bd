{
      "bsonType": "object",
      "required": ["userName", "registerEmail", "registerPassword"],
      "properties": {
        "userName": {
          "bsonType": "string",
          "description": "must be a string and is required",
          "uniqueItems": true
        },
        "registerEmail": {
          "bsonType": "string",
          "description": "must be a string and is required",
          "uniqueItems": true
        },
        "registerPassword": {
          "bsonType": "string",
          "description": "must be a string and is required"
        }
      }
  }